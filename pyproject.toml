[project]
name = "fyproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sudhanshunitinatalkar", email = "atalkarsudhanshu@proton.me" }
]
requires-python = ">=3.14"
dependencies = [ "cactus-needle" ]

[project.scripts]
fyproject = "fyproject:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
